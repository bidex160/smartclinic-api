import { INestApplication, UnauthorizedException } from '@nestjs/common';import{Test}from'@nestjs/testing';import*as request from'supertest';import{PublicBookingsController}from'../src/bookings/public-bookings.controller';import{PublicBookingsService}from'../src/bookings/public-bookings.service';import{PUBLIC_BOOKING_SESSION_COOKIE,PublicBookingSessionService}from'../src/bookings/public-booking-session.service';import{PaymentFlowService}from'../src/payments/payment-flow.service';
describe('Public Paystack initiation boundary (e2e)',()=>{let app:INestApplication;const reference='SC-2026-ABCDEF123456';beforeAll(async()=>{const sessions={resolveBooking:jest.fn(async(token:string|null)=>{if(token!=='session')throw new UnauthorizedException();return{};}),cookieOptions:jest.fn()};const payments={initializeFunding:jest.fn().mockResolvedValue({}),initiatePublicPayment:jest.fn().mockResolvedValue({bookingReference:reference,fundingStatus:'PENDING',attemptId:'internal-id',attemptStatus:'AWAITING_CUSTOMER_ACTION',amount:'12500.00',currency:'NGN',paymentReference:'SC-PAY-safe',checkoutUrl:'https://checkout.paystack.test/safe'})};const module=await Test.createTestingModule({controllers:[PublicBookingsController],providers:[{provide:PublicBookingsService,useValue:{}},{provide:PublicBookingSessionService,useValue:sessions},{provide:PaymentFlowService,useValue:payments}]}).compile();app=module.createNestApplication();app.setGlobalPrefix('api/v1');await app.init();});afterAll(async()=>app.close());const path=`/api/v1/public/bookings/${reference}/payment/initiate`;it('denies initiation without the booking session',()=>request(app.getHttpServer()).post(path).expect(401));it('returns only safe checkout data for the controlled booking',()=>request(app.getHttpServer()).post(path).set('Cookie',`${PUBLIC_BOOKING_SESSION_COOKIE}=session`).expect(200).expect((r)=>{expect(r.body).toEqual({bookingReference:reference,paymentAttemptReference:'SC-PAY-safe',status:'AWAITING_CUSTOMER_ACTION',amount:'12500.00',currency:'NGN',checkoutUrl:'https://checkout.paystack.test/safe'});expect(r.body).not.toHaveProperty('attemptId');}));});
