import { INestApplication, UnauthorizedException, ValidationPipe } from '@nestjs/common'; import { Reflector } from '@nestjs/core'; import { Test } from '@nestjs/testing'; import * as request from 'supertest'; import { JwtAuthGuard } from '../src/auth/jwt-auth.guard'; import { RolesGuard } from '../src/auth/roles.guard'; import { UserRole } from '../src/users/enums/user-role.enum'; import { AdminPaymentFlowController } from '../src/payments/admin-payment-flow.controller'; import { PaymentFlowService } from '../src/payments/payment-flow.service';
describe('Admin payment flow authorization (e2e)',()=>{let app:INestApplication;const reference='SC-2026-ABCDEF123456';beforeAll(async()=>{const service={initializeFunding:jest.fn().mockResolvedValue({bookingReference:reference,fundingStatus:'PENDING',attemptId:null,attemptStatus:null,amount:'12500.00',currency:'NGN',paymentReference:null}),initiatePayment:jest.fn(),confirmPayment:jest.fn()};const module=await Test.createTestingModule({controllers:[AdminPaymentFlowController],providers:[RolesGuard,Reflector,{provide:PaymentFlowService,useValue:service}]}).overrideGuard(JwtAuthGuard).useValue({canActivate:(context:any)=>{const req=context.switchToHttp().getRequest();const token=req.headers.authorization;if(!token)throw new UnauthorizedException();req.user={id:'10000000-0000-4000-8000-000000000001',roles:token==='Bearer admin'?[UserRole.ADMIN]:token==='Bearer operations'?[UserRole.OPERATIONS]:[UserRole.USER]};return true;}}).compile();app=module.createNestApplication();app.setGlobalPrefix('api/v1');app.useGlobalPipes(new ValidationPipe({whitelist:true,transform:true}));await app.init();});afterAll(async()=>app.close());const path=`/api/v1/admin/bookings/${reference}/funding/initialize`;it('returns 401 unauthenticated',()=>request(app.getHttpServer()).post(path).expect(401));it('returns 403 for USER',()=>request(app.getHttpServer()).post(path).set('Authorization','Bearer user').expect(403));it('allows ADMIN',()=>request(app.getHttpServer()).post(path).set('Authorization','Bearer admin').expect(200));it('allows OPERATIONS',()=>request(app.getHttpServer()).post(path).set('Authorization','Bearer operations').expect(200));});
