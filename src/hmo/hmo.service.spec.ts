import { BadRequestException,ConflictException } from '@nestjs/common'; import { HmoService } from './hmo.service';
describe('HmoService Phase 1 gates',()=>{const hmos:any={findOneBy:jest.fn().mockResolvedValue({id:'h1',active:true}),save:jest.fn(async(x:any)=>x),create:(x:any)=>x};const plans:any={findOneBy:jest.fn(),save:jest.fn(async(x:any)=>x),create:(x:any)=>x};const coverages:any={findOne:jest.fn(),save:jest.fn(async(x:any)=>x),create:(x:any)=>x};const cases:any={findOne:jest.fn(),findOneBy:jest.fn(),save:jest.fn(async(x:any)=>x),create:(x:any)=>x};const auths:any={findOneBy:jest.fn(),save:jest.fn(async(x:any)=>x),create:(x:any)=>x};const claims:any={findOneBy:jest.fn(),save:jest.fn(async(x:any)=>x),create:(x:any)=>x};const cares:any={findOneBy:jest.fn()};const access:any={resolveAccessiblePatient:jest.fn()};let s:HmoService;beforeEach(()=>{jest.clearAllMocks();s=new HmoService(hmos,plans,coverages,cases,auths,claims,cares,access);});
it('does not request PA until encounter eligibility is Eligible',async()=>{cases.findOneBy.mockResolvedValue({id:'c1',eligibilityStatus:'PENDING'});await expect(s.requestAuthorization('SCHMO-1',{requestedServices:[{name:'Consultation'}]} as any)).rejects.toBeInstanceOf(ConflictException);expect(auths.save).not.toHaveBeenCalled();});
it('requires a PA code for approved authorization',async()=>{auths.findOneBy.mockResolvedValue({reference:'SCPA-1'});await expect(s.decide('SCPA-1',{status:'APPROVED',approvedServices:[]} as any)).rejects.toBeInstanceOf(BadRequestException);});
it('does not generate a claim from authorization alone',async()=>{auths.findOneBy.mockResolvedValue({id:'a1',status:'APPROVED',serviceConfirmedAt:null,deliveredServices:[]});await expect(s.generateClaim('SCPA-1')).rejects.toBeInstanceOf(ConflictException);expect(claims.save).not.toHaveBeenCalled();});
it('generates one claim only after delivered services are explicitly confirmed',async()=>{auths.findOneBy.mockResolvedValue({id:'a1',status:'APPROVED',serviceConfirmedAt:new Date(),deliveredServices:[{name:'Consultation'}],approvedAmountMinor:'500000',requestedAmountMinor:'500000',currency:'NGN'});claims.findOneBy.mockResolvedValue(null);await expect(s.generateClaim('SCPA-1')).resolves.toMatchObject({status:'DRAFT',claimedAmountMinor:'500000'});expect(claims.save).toHaveBeenCalledTimes(1);});
it('prevents a coverage record from being attached through another patient identity',async()=>{access.resolveAccessiblePatient.mockRejectedValue(new Error('not accessible'));await expect(s.addMine('u1','SCP-OTHER',{hmoId:'h1',memberId:'LW-001'} as any)).rejects.toThrow();expect(coverages.save).not.toHaveBeenCalled();});});