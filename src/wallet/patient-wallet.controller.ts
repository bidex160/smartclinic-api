import { Controller,Get,Req,UseGuards } from '@nestjs/common';import { ApiBearerAuth,ApiTags } from '@nestjs/swagger';import { JwtAuthGuard } from '../auth/jwt-auth.guard';import { Roles } from '../auth/roles.decorator';import { RolesGuard } from '../auth/roles.guard';import { User } from '../users/entities/user.entity';import { UserRole } from '../users/enums/user-role.enum';import { PatientWalletService } from './patient-wallet.service';
@ApiTags('My Wallet')@ApiBearerAuth()@UseGuards(JwtAuthGuard,RolesGuard)@Roles(UserRole.USER)@Controller('me/wallet')export class PatientWalletController{constructor(private readonly wallet:PatientWalletService){}@Get()mine(@Req()r:{user:User}){return this.wallet.mine(r.user.id);}}