import { BadRequestException, ConflictException } from '@nestjs/common';
import { BookingStatusHistory } from '../bookings/entities/booking-status-history.entity';
import { Booking } from '../bookings/entities/booking.entity';
import { BookingStatus } from '../bookings/enums/booking-status.enum';
import { ProviderAssignmentHistory } from './entities/provider-assignment-history.entity';
import { ProviderAssignment } from './entities/provider-assignment.entity';
import { ProviderAssignmentStatus } from './enums/provider-assignment-status.enum';
import { ProviderMatchingService } from './provider-matching.service';
import { ProviderBookingReservation } from './entities/provider-booking-reservation.entity';
import { ProviderBookingReservationStatus } from './enums/provider-booking-reservation-status.enum';
import { Provider } from './entities/provider.entity';
import { ProviderStatus } from './enums/provider-status.enum';

const ids = { booking: '10000000-0000-4000-8000-000000000001', provider1: '20000000-0000-4000-8000-000000000001', provider2: '20000000-0000-4000-8000-000000000002', assignment: '30000000-0000-4000-8000-000000000001', actor: '40000000-0000-4000-8000-000000000001' };
const now = new Date('2026-08-24T08:00:00.000Z');

describe('ProviderMatchingService', () => {
  let bookings: any, assignments: any, reservations: any, bookingHistory: any, assignmentHistory: any, capabilities: any, subject: ProviderMatchingService;
  const booking = (changes = {}) => ({ id: ids.booking, bookingReference: 'SC-2026-ABCDEFGHIJKL', status: BookingStatus.PENDING_PROVIDER_MATCH, healthCheckPackageId: '50000000-0000-4000-8000-000000000001', fulfilmentModeId: '60000000-0000-4000-8000-000000000001', preferredDate: '2026-08-24', preferredTimeWindowStart: '09:00', preferredTimeWindowEnd: '11:00', preferredTimezone: 'Africa/Lagos', healthCheckPackage: { isActive: true }, fulfilmentMode: { isActive: true }, ...changes }) as Booking;
  const offer = (changes = {}) => ({ id: ids.assignment, bookingId: ids.booking, providerId: ids.provider1, status: ProviderAssignmentStatus.OFFERED, offeredAt: now, expiresAt: new Date(now.getTime() + 30 * 60_000), respondedAt: null, acceptedAt: null, confirmedAt: null, reasonCode: null, reasonNote: null, booking: booking(), ...changes }) as ProviderAssignment;
  const eligible = (providerId: string) => ({ id: `service-${providerId}`, providerId, healthCheckPackageId: 'package', fulfilmentModeId: 'mode', isActive: true, providerLocationIds: [], createdAt: now, updatedAt: now });

  beforeEach(() => {
    bookingHistory = { create: jest.fn((value) => value), save: jest.fn(async (value) => value) };
    assignmentHistory = { create: jest.fn((value) => value), save: jest.fn(async (value) => value) };
    bookings = { findOne: jest.fn().mockResolvedValue(booking()), save: jest.fn(async (value) => value) };
    assignments = { find: jest.fn().mockResolvedValue([]), findOne: jest.fn(async (options: any) => options.where?.status ? null : offer()), exists: jest.fn().mockResolvedValue(false), create: jest.fn((value) => value), save: jest.fn(async (value) => ({ id: value.id ?? ids.assignment, ...value })) };
    const reservationQuery = { where: jest.fn().mockReturnThis(), andWhere: jest.fn().mockReturnThis(), getExists: jest.fn().mockResolvedValue(false) };
    reservations = { findOne: jest.fn().mockResolvedValue({ id: 'reservation', providerAssignmentId: ids.assignment, status: ProviderBookingReservationStatus.HELD, releasedAt: null }), create: jest.fn((value) => value), save: jest.fn(async (value) => ({ id: 'reservation', ...value })), createQueryBuilder: jest.fn().mockReturnValue(reservationQuery) };
    const providers = { findOne: jest.fn().mockResolvedValue({ id: ids.provider1, status: ProviderStatus.ACTIVE, deletedAt: null }) };
    const manager: any = { getRepository: jest.fn((entity) => entity === Booking ? bookings : entity === ProviderAssignment ? assignments : entity === ProviderBookingReservation ? reservations : entity === BookingStatusHistory ? bookingHistory : entity === Provider ? providers : assignmentHistory) };
    manager.transaction = jest.fn(async (work: (value: any) => unknown) => work(manager));
    assignments.manager = manager;
    capabilities = { findEligibleProviders: jest.fn().mockResolvedValue([eligible(ids.provider1), eligible(ids.provider2)]) };
    subject = new ProviderMatchingService(bookings, assignments, capabilities, { providerMatching: { offerTtlMinutes: 30 } } as never);
  });

  it('requires complete booking scheduling context', async () => { bookings.findOne.mockResolvedValue(booking({ preferredTimezone: null })); await expect(subject.startMatching('SC-2026-ABCDEFGHIJKL', ids.actor, now)).rejects.toBeInstanceOf(BadRequestException); });
  it.each([BookingStatus.DRAFT, BookingStatus.AWAITING_FUNDING, BookingStatus.COMPLETED, BookingStatus.CANCELLED, BookingStatus.EXPIRED, BookingStatus.IN_PROGRESS])('rejects ineligible booking state %s', async (status) => { bookings.findOne.mockResolvedValue(booking({ status })); await expect(subject.startMatching('SC-2026-ABCDEFGHIJKL', ids.actor, now)).rejects.toBeInstanceOf(ConflictException); });
  it('discovers eligible providers and creates only the first sequential offer with configured expiry and history', async () => {
    const result = await subject.startMatching('SC-2026-ABCDEFGHIJKL', ids.actor, now);
    expect(capabilities.findEligibleProviders).toHaveBeenCalledWith(expect.any(String), expect.any(String), expect.objectContaining({ requestedTimezone: 'Africa/Lagos' }));
    expect(result.assignment).toMatchObject({ providerId: ids.provider1, status: ProviderAssignmentStatus.OFFERED, expiresAt: new Date('2026-08-24T08:30:00.000Z') });
    expect(assignments.save).toHaveBeenCalledTimes(1);
    expect(assignmentHistory.save).toHaveBeenCalledWith(expect.objectContaining({ fromStatus: null, toStatus: ProviderAssignmentStatus.OFFERED }));
  });
  it('moves a booking to UNFULFILLABLE with history when no eligible provider remains', async () => { capabilities.findEligibleProviders.mockResolvedValue([]); const result = await subject.startMatching('SC-2026-ABCDEFGHIJKL', ids.actor, now); expect(result).toEqual({ bookingStatus: BookingStatus.UNFULFILLABLE, assignment: null }); expect(bookingHistory.save).toHaveBeenCalledWith(expect.objectContaining({ fromStatus: BookingStatus.PENDING_PROVIDER_MATCH, toStatus: BookingStatus.UNFULFILLABLE })); });
  it('retries an unfulfillable booking and records the return to matching when a new provider is eligible', async () => { bookings.findOne.mockResolvedValue(booking({ status: BookingStatus.UNFULFILLABLE })); const result = await subject.retryMatching('SC-2026-ABCDEFGHIJKL', ids.actor, now); expect(result.assignment).toMatchObject({ providerId: ids.provider1 }); expect(bookingHistory.save).toHaveBeenCalledWith(expect.objectContaining({ fromStatus: BookingStatus.UNFULFILLABLE, toStatus: BookingStatus.PENDING_PROVIDER_MATCH, reasonCode: 'MATCHING_RETRIED' })); });
  it('treats a duplicate automatic trigger as idempotent when an active offer exists', async () => { assignments.findOne.mockResolvedValueOnce(offer()); const result = await subject.startMatching('SC-2026-ABCDEFGHIJKL', null, now); expect(result.assignment).toMatchObject({ id: ids.assignment, status: ProviderAssignmentStatus.OFFERED }); expect(assignments.save).not.toHaveBeenCalled(); });
  it('manually offers only to a selected normally eligible provider and rejects capability, availability, or capacity exclusions', async () => { const result = await subject.assignEligibleProvider('SC-2026-ABCDEFGHIJKL', ids.provider2, ids.actor, now); expect(result.assignment).toMatchObject({ providerId: ids.provider2, reasonCode: 'MANUAL_PROVIDER_ASSIGNED' }); capabilities.findEligibleProviders.mockResolvedValue([]); await expect(subject.assignEligibleProvider('SC-2026-ABCDEFGHIJKL', ids.provider2, ids.actor, now)).rejects.toBeInstanceOf(ConflictException); });
  it('serializes auto/manual matching so a manual race cannot create a second active offer', async () => { assignments.findOne.mockResolvedValueOnce(offer()); await expect(subject.assignEligibleProvider('SC-2026-ABCDEFGHIJKL', ids.provider2, ids.actor, now)).rejects.toBeInstanceOf(ConflictException); expect(assignments.save).not.toHaveBeenCalled(); });
  it('records an explicit override reason while retaining active-provider and lifecycle checks', async () => { const result = await subject.assignProviderOverride('SC-2026-ABCDEFGHIJKL', ids.provider1, 'Operations exception', ids.actor, now); expect(result.assignment).toMatchObject({ providerId: ids.provider1, reasonCode: 'MANUAL_PROVIDER_OVERRIDE', reasonNote: 'Operations exception' }); expect(assignmentHistory.save).toHaveBeenCalledWith(expect.objectContaining({ actorUserId: ids.actor, reasonCode: 'MANUAL_PROVIDER_OVERRIDE', reasonNote: 'Operations exception' })); bookings.findOne.mockResolvedValue(booking({ status: BookingStatus.COMPLETED })); await expect(subject.assignProviderOverride('SC-2026-ABCDEFGHIJKL', ids.provider1, 'Operations exception', ids.actor, now)).rejects.toBeInstanceOf(ConflictException); });
  it('rejects override assignment to an inactive provider', async () => { assignments.manager.getRepository(Provider).findOne.mockResolvedValue({ id: ids.provider1, status: ProviderStatus.SUSPENDED }); await expect(subject.assignProviderOverride('SC-2026-ABCDEFGHIJKL', ids.provider1, 'Operations exception', ids.actor, now)).rejects.toBeInstanceOf(ConflictException); });
  it('does not let an override bypass an existing capacity reservation', async () => { reservations.createQueryBuilder().getExists.mockResolvedValueOnce(true); await expect(subject.assignProviderOverride('SC-2026-ABCDEFGHIJKL', ids.provider1, 'Operations exception', ids.actor, now)).rejects.toBeInstanceOf(ConflictException); expect(assignments.save).not.toHaveBeenCalled(); });
  it('requires an override reason and lets an active override provider accept without pretending normal eligibility', async () => { await expect(subject.assignProviderOverride('SC-2026-ABCDEFGHIJKL', ids.provider1, ' ', ids.actor, now)).rejects.toBeInstanceOf(BadRequestException); capabilities.findEligibleProviders.mockResolvedValue([]); assignments.findOne.mockResolvedValue(offer({ reasonCode: 'MANUAL_PROVIDER_OVERRIDE' })); await expect(subject.acceptOffer(ids.assignment, ids.provider1, now)).resolves.toMatchObject({ status: ProviderAssignmentStatus.ACCEPTED }); expect(reservations.save).toHaveBeenCalled(); });
  it('accepts an owned unexpired offer, creates the mapped HELD reservation, and appends history', async () => { const result = await subject.acceptOffer(ids.assignment, ids.provider1, now); expect(result.status).toBe(ProviderAssignmentStatus.ACCEPTED); expect(reservations.save).toHaveBeenCalledWith(expect.objectContaining({ providerId: ids.provider1, bookingId: ids.booking, providerAssignmentId: ids.assignment, scheduledDate: '2026-08-24', startTime: '09:00', endTime: '11:00', timezone: 'Africa/Lagos', status: ProviderBookingReservationStatus.HELD })); expect(assignmentHistory.save).toHaveBeenCalledWith(expect.objectContaining({ toStatus: ProviderAssignmentStatus.ACCEPTED })); });
  it('leaves an offer unchanged when capacity eligibility fails', async () => { capabilities.findEligibleProviders.mockResolvedValue([]); await expect(subject.acceptOffer(ids.assignment, ids.provider1, now)).rejects.toBeInstanceOf(ConflictException); expect(assignments.save).not.toHaveBeenCalled(); expect(assignments.findOne.mock.results[0].value).resolves.toMatchObject({ status: ProviderAssignmentStatus.OFFERED }); });
  it('rejects acceptance by a different provider or after expiry', async () => { await expect(subject.acceptOffer(ids.assignment, ids.provider2, now)).rejects.toBeInstanceOf(ConflictException); assignments.findOne.mockResolvedValue(offer({ expiresAt: now })); await expect(subject.acceptOffer(ids.assignment, ids.provider1, now)).rejects.toBeInstanceOf(ConflictException); });
  it('rejects an offer closed by booking cancellation or rescheduling', async () => { assignments.findOne.mockResolvedValue(offer({ status: ProviderAssignmentStatus.CANCELLED, booking: booking({ status: BookingStatus.CANCELLED }) })); await expect(subject.acceptOffer(ids.assignment, ids.provider1, now)).rejects.toBeInstanceOf(ConflictException); });
  it('declines an offer without reserving capacity and creates the next sequential offer', async () => { assignments.find.mockResolvedValue([offer({ status: ProviderAssignmentStatus.DECLINED })]); const result = await subject.declineOffer(ids.assignment, ids.provider1, 'Unavailable', now); expect(result.declined.status).toBe(ProviderAssignmentStatus.DECLINED); expect(result.next.assignment?.providerId).toBe(ids.provider2); expect(reservations.save).not.toHaveBeenCalled(); expect(assignmentHistory.save).toHaveBeenCalledWith(expect.objectContaining({ toStatus: ProviderAssignmentStatus.DECLINED, reasonNote: 'Unavailable' })); });
  it('confirms an accepted assignment, promotes HELD capacity, advances the booking, and appends both histories', async () => { assignments.findOne.mockResolvedValue(offer({ status: ProviderAssignmentStatus.ACCEPTED, acceptedAt: now })); const result = await subject.confirmAssignment(ids.assignment, ids.actor, now); expect(result.status).toBe(ProviderAssignmentStatus.CONFIRMED); expect(reservations.save).toHaveBeenCalledWith(expect.objectContaining({ status: ProviderBookingReservationStatus.CONFIRMED })); expect(assignmentHistory.save).toHaveBeenCalledWith(expect.objectContaining({ toStatus: ProviderAssignmentStatus.CONFIRMED })); expect(bookingHistory.save).toHaveBeenCalledWith(expect.objectContaining({ toStatus: BookingStatus.PROVIDER_ASSIGNED })); });
  it('locks assignments and bookings separately without relation-loaded FOR UPDATE queries', async () => { await subject.acceptOffer(ids.assignment, ids.provider1, now); const lockedAssignmentCalls = assignments.findOne.mock.calls.filter(([options]: any[]) => options.lock); expect(lockedAssignmentCalls).not.toHaveLength(0); for (const [options] of lockedAssignmentCalls) expect(options).not.toHaveProperty('relations'); expect(bookings.findOne).toHaveBeenCalledWith(expect.objectContaining({ where: { id: ids.booking }, lock: { mode: 'pessimistic_write' } })); });
  it('releases active reservations for future cancellation workflows', async () => { await subject.releaseReservationForAssignment(ids.assignment, false, now); expect(reservations.save).toHaveBeenCalledWith(expect.objectContaining({ status: ProviderBookingReservationStatus.RELEASED, releasedAt: now })); });
  it('prevents a second confirmed assignment', async () => { assignments.findOne.mockResolvedValue(offer({ status: ProviderAssignmentStatus.ACCEPTED })); assignments.exists.mockResolvedValue(true); await expect(subject.confirmAssignment(ids.assignment, ids.actor, now)).rejects.toBeInstanceOf(ConflictException); });
  it('expires stale offers without reserving capacity and continues to the next provider', async () => { const stale = offer({ expiresAt: now, booking: booking() }); assignments.find.mockResolvedValueOnce([stale]).mockResolvedValueOnce([stale]); const result = await subject.expireStaleOffers(ids.actor, now); expect(result.expiredCount).toBe(1); expect(result.nextOffers[0].assignment?.providerId).toBe(ids.provider2); expect(reservations.save).not.toHaveBeenCalled(); expect(assignmentHistory.save).toHaveBeenCalledWith(expect.objectContaining({ toStatus: ProviderAssignmentStatus.EXPIRED })); });
  it('reassigns confirmed work transactionally, releases capacity, and offers the selected eligible provider', async () => { const current = offer({ status: ProviderAssignmentStatus.CONFIRMED }); bookings.findOne.mockResolvedValue(booking({ status: BookingStatus.PROVIDER_ASSIGNED })); assignments.findOne.mockImplementation(async (options: any) => options.where?.status && current.status !== ProviderAssignmentStatus.CANCELLED ? current : options.where?.status ? null : offer()); const result = await subject.reassign('SC-2026-ABCDEFGHIJKL', ids.actor, 'Provider unavailable', ids.provider2, now); expect(current.status).toBe(ProviderAssignmentStatus.CANCELLED); expect(reservations.save).toHaveBeenCalledWith(expect.objectContaining({ status: ProviderBookingReservationStatus.RELEASED })); expect(bookingHistory.save).toHaveBeenCalledWith(expect.objectContaining({ toStatus: BookingStatus.PENDING_PROVIDER_MATCH, reasonCode: 'PROVIDER_REASSIGNED' })); expect(result.assignment).toMatchObject({ providerId: ids.provider2 }); });
  it.each([BookingStatus.IN_PROGRESS, BookingStatus.COMPLETED])('protects %s bookings from reassignment', async (status) => { const current = offer({ status: ProviderAssignmentStatus.CONFIRMED }); bookings.findOne.mockResolvedValue(booking({ status })); assignments.findOne.mockResolvedValue(current); await expect(subject.reassign('SC-2026-ABCDEFGHIJKL', ids.actor, 'Operations correction', undefined, now)).rejects.toBeInstanceOf(ConflictException); expect(current.status).toBe(ProviderAssignmentStatus.CONFIRMED); });
});
