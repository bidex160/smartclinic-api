import { Body,Controller,Get,HttpCode,HttpStatus,Param,Post,Req,UseGuards } from '@nestjs/common';import { ApiBearerAuth,ApiTags } from '@nestjs/swagger';import { JwtAuthGuard } from '../auth/jwt-auth.guard';import { Roles } from '../auth/roles.decorator';import { RolesGuard } from '../auth/roles.guard';import { PharmacyQuoteReferenceParamsDto } from '../clinical-orders/dto/pharmacy-quote.dto';import { User } from '../users/entities/user.entity';import { UserRole } from '../users/enums/user-role.enum';import { PaymentContactDto } from './dto/initiate-payment.dto';import { PaymentFlowService } from './payment-flow.service';
@ApiTags('My Pharmacy Funding')@ApiBearerAuth()@UseGuards(JwtAuthGuard,RolesGuard)@Roles(UserRole.USER)@Controller('me/pharmacy-quotes/:reference/funding')export class MePharmacyFundingController{constructor(private readonly s:PaymentFlowService){}@Get()get(@Req()r:{user:User},@Param()p:PharmacyQuoteReferenceParamsDto){return this.s.getPharmacyFunding(p.reference,r.user.id);}@Post('initialize')@HttpCode(HttpStatus.OK)initialize(@Req()r:{user:User},@Param()p:PharmacyQuoteReferenceParamsDto,@Body()dto:PaymentContactDto){return this.s.initializePharmacyFunding(p.reference,r.user.id,dto?.paymentEmail);}@Post('verify-latest')@HttpCode(HttpStatus.OK)verify(@Req()r:{user:User},@Param()p:PharmacyQuoteReferenceParamsDto){return this.s.verifyLatestPharmacyFunding(p.reference,r.user.id);}}
