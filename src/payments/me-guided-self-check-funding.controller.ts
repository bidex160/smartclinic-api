import { Controller,Get,HttpCode,HttpStatus,Param,Post,Req,UseGuards } from '@nestjs/common'; import { ApiBearerAuth,ApiTags } from '@nestjs/swagger'; import { JwtAuthGuard } from '../auth/jwt-auth.guard'; import { Roles } from '../auth/roles.decorator'; import { RolesGuard } from '../auth/roles.guard'; import { User } from '../users/entities/user.entity'; import { UserRole } from '../users/enums/user-role.enum'; import { PaymentFlowService } from './payment-flow.service';
@ApiTags('My Guided Self-Check funding') @ApiBearerAuth() @UseGuards(JwtAuthGuard,RolesGuard) @Roles(UserRole.USER) @Controller('me/guided-self-checks/:reference/funding') export class MeGuidedSelfCheckFundingController {constructor(private s:PaymentFlowService){}@Get()get(@Param('reference')ref:string,@Req()r:{user:User}){return this.s.getGuidedSelfCheckFunding(ref,r.user.id);}@Post('initialize')@HttpCode(HttpStatus.OK)initialize(@Param('reference')ref:string,@Req()r:{user:User}){return this.s.initializeGuidedSelfCheckFunding(ref,r.user.id);}@Post('verify-latest')@HttpCode(HttpStatus.OK)verify(@Param('reference')ref:string,@Req()r:{user:User}){return this.s.verifyLatestGuidedSelfCheckFunding(ref,r.user.id);}}
